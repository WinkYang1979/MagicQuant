# MagicQuant 05-18 事故复盘与修复建议（Codex 版）

日期：2026-05-18  
对象：MagicQuant / RKLB-RKLX-RKLZ Focus 系统  
目的：供 Claude 对照分析，互相印证修复方向  

---

## 0. 结论先行

这次事故不应再被归因成“策略阈值不够好”或“信心阈值需要微调”。

真正的问题是：

> 系统没有先证明数据可信，就生成了高信心交易建议。

05-18 的 95% 强烈看空信号，不是因为策略判断错了一点，而是因为它使用了已经过期的 K_5M 指标数据：

- 05-18 14:03 推送方向信号
- 推送内容显示 RSI=43.1、vol_ratio=4.94
- `triggers.json` 里 `is_today=false`
- `kline_data.bars` 最后三根是 2026-05-15 15:50、15:55、16:00
- 也就是说，系统在 2026-05-18 用 2026-05-15 的 5 分钟 K 线生成了 95% 强烈看空

所以核心修复原则应该是：

> 先证明数据可信，再谈信号强弱。  
> 先保护用户不被误导，再谈捕捉机会。  
> 宁可少推，也不能错推强信号。

---

## 1. 关于“美股交易时间”的修正

需要特别说明：不能把这次事故简单说成“美股休市时系统推了信号”。

用户指出得对：富途 / moomoo 支持美股 24/5 交易，覆盖夜盘、盘前、盘中、盘后。白天也可能是美股夜盘或延长交易时段，不能用传统 09:30-16:00 ET 作为唯一判断标准。

官方资料显示：

- 美股常规盘：周一至周五 09:30-16:00 ET
- 富途夜盘：周日至周四 20:00-04:00 ET
- 富途支持美股 24/5，包括夜盘、盘前、盘后
- 夜盘 / 延长交易时段存在更低流动性、更大价差、更高波动风险

参考：

- Futu US Stock Trading: https://www.futuhk.com/en/products/us-stocks
- Futu 5x24 夜盘说明: https://www.futunn.com/en/learn/detail-5x24-hours-the-us-stock-trading-night-market-is-uninterrupted-78889-230334138
- NYSE Extended Hours: https://www.nyse.com/extended-hours-trading

因此修复不能写成：

```text
market_status != regular 时禁止方向信号
```

这会误杀夜盘 / 盘前 / 盘后场景。

正确逻辑应该是：

```text
交易时段不是硬门槛；
数据新鲜度才是硬门槛。
```

24h 可交易，不等于 24h 都能使用同一套 RSI / VWAP / vol_ratio 策略。

---

## 2. 05-18 事故链条

### 2.1 14:03 强烈看空信号

系统推送：

```text
RKLB 方向信号（看空）[强烈]
信心：95%
方向：偏空，适合做空 RKLZ
RKLB $123.60 (-6.75%)
RKLX $79.28 (-13.47%)
RKLZ $2.70 (+13.92%)
RSI 43.1 · VWAP $123.60 · 量比 4.94x

可考虑介入：RKLZ × 1037 股 @ $2.70
```

用户随后买入 RKLZ。

### 2.2 实际市场

之后 RKLB 反弹，RKLZ 下跌。

19:05 心跳显示：

```text
现价 $129.42 日内 -2.36%
今日区间 $123.35 ~ $130.19
RSI 43.1 VWAP $129.42 量比 4.94x

当前处于震荡市，方向信号被压制
看空已满足（日内 -2.36%）

持仓：
RKLZ: 500股 $-125.00 (-9.23%)
```

这里暴露出几个问题：

- RSI / vol_ratio 仍是 43.1 / 4.94，没有更新
- 心跳知道持仓亏损，却没有识别上一条看空信号可能失效
- “方向信号被压制”和“看空已满足”同时出现，语义冲突
- 数据异常没有变成最高优先级状态

---

## 3. 我检查到的代码层问题

### 3.1 `micro_indicators.py`

问题：

- `is_today=False` 时仍可能返回 `data_ok=True`
- RSI / vol_ratio 会从 full K 线计算，即使这些 K 线属于前一个交易日
- VWAP 在没有今日数据时可能 fallback 成 `current_price`
- 下游看到 `data_ok=True` 后误以为技术指标可用

直接后果：

```text
旧 K 线指标被包装成“可用指标”。
```

### 3.2 `swing_detector.py`

问题：

- `check_direction_trend()` 主要检查 `data_ok`
- 没有要求 `is_today=True`
- 没有检查最后 K 线时间戳是否足够新
- v0.5.34 的冻结检测只检查 vol_ratio 连续 3 次重复
- 这个重复检测依赖历史样本，挡不住当天第一条错误强信号

05-18 正是当天第一条危险信号：

```text
14:03 第一条 direction_trend 已经推出
后面的重复检测即使生效，也已经晚了
```

### 3.3 `pusher.py`

问题：

- 没有最终数据质量门禁
- 只要 hit 被生成，就会格式化并发送
- 一键下单按钮也随信号生成

这次最危险的不是只有文案，而是出现了：

```text
买 1037股 RKLZ
```

这类按钮必须单独加门禁。

### 3.4 `focus_manager.py`

问题：

- 已经能看到 `bar_is_today=False`
- 但把它更像是当成“盘前等待今日 K 线”的状态
- 没有把这个状态传递为“方向信号不可用”
- `calc_all_micro()` 仍然会基于旧 K 线给出 `data_ok=True`

换句话说：

```text
采集层知道 K 线不是今天，
但信号层仍然认为指标可用。
```

### 3.5 `heartbeat.py`

问题：

- 部分逻辑能识别 `is_today=False`
- 但后续仍会显示“看空已满足”等触发器状态
- 坏数据状态没有成为 heartbeat 的总裁决

坏数据时，heartbeat 应该进入 `DATA_UNTRUSTED` 模式，不应该继续展示方向触发距离。

---

## 4. 根因总结

### 根因 1：数据可信度没有成为推送硬门槛

系统现在大致是：

```text
行情数据 → 指标计算 → 策略判断 → 推送
```

缺少：

```text
行情数据 → 数据可信度门禁 → 指标计算 / 策略判断 → 推送
```

也缺少：

```text
推送前最后一道数据可信度门禁
```

### 根因 2：`data_ok` 语义太弱

当前 `data_ok=True` 只表示“可以算出指标”，不等于“指标适合当前交易决策”。

应该区分：

- `can_compute_indicators`
- `is_fresh`
- `is_current_session`
- `can_direction`
- `can_entry`
- `can_risk`

### 根因 3：24h 可交易和 24h 指标可用被混为一谈

富途支持夜盘 / 盘前 / 盘后，但这不代表：

- K_5M 一定实时更新
- VWAP 一定适用于当前 session
- RSI 一定来自当前交易日 / 当前交易段
- regular session 的量比逻辑可以无修改用于 overnight

### 根因 4：按钮风险没有独立控制

一键按钮是比文字建议更高风险的交互。

只要数据质量不可信，就不应该生成任何 order button。

### 根因 5：系统没有跟踪“自己刚刚推荐的方向是否失效”

系统推荐 RKLZ 后，用户实际持有 RKLZ。

当 RKLB 反向上涨、RKLZ 开始亏损时，系统应该尽早提示：

```text
上一条看空信号可能失效。
```

而不是等到硬止损。

---

## 5. 全盘修复建议

## 5.1 新增 Session-Aware Data Quality Gate

建议新增模块：

```text
core/focus/data_quality.py
```

它的职责不是判断涨跌，而是回答：

```text
当前这组数据能不能用于生成方向 / 买入 / 止盈 / 抄底信号？
```

建议返回结构：

```python
{
    "ok": bool,
    "level": "OK" | "WARN" | "BLOCK",
    "reason": "...",
    "session": "overnight" | "premarket" | "regular" | "afterhours" | "closed" | "unknown",
    "can_direction": bool,
    "can_entry": bool,
    "can_exit": bool,
    "can_risk": bool,
    "can_heartbeat": bool,
    "last_bar_time": "...",
    "last_bar_age_sec": 0,
    "is_current_trading_day": bool,
}
```

必须检查：

1. `data_ok == True`
2. K 线最后时间存在
3. K 线最后时间属于当前交易日或当前 overnight session
4. K 线最后更新时间没有超过当前 session 允许的最大延迟
5. RSI / vol_ratio 没有连续 3 次完全相同
6. VWAP 不是 fallback 出来的伪 VWAP
7. 数据源 session 与策略 session 匹配
8. 已知冻结组合只作为临时保险，不作为主逻辑

重点：

```text
不要用“是否 regular”决定能不能交易；
用“当前 session 的数据是否真实更新”决定能不能推信号。
```

---

## 5.2 信号分级处理

建议把 trigger 分成三类。

### A 类：高风险交易建议，必须强门禁

包括：

- `direction_trend`
- `swing_bottom`
- `near_support`
- `rapid_move`
- `target_advance`
- `profit_target_hit`
- 一键买入 / 加仓 / 做空按钮

要求：

```text
data_quality.can_direction == True
或 data_quality.can_entry == True
```

否则直接阻断，不推送。

### B 类：持仓风险，可在坏数据下保留

包括：

- `stop_loss_warning`
- 持仓浮亏提醒
- 账户风险提醒

但文案必须标记：

```text
⚠️ 技术指标数据不可用
本提醒仅基于实时价格 / 持仓盈亏，不代表方向判断。
```

### C 类：状态播报

包括：

- heartbeat
- status
- data_quality_alert

坏数据时可以推，但必须进入 `DATA_UNTRUSTED` 模式。

禁止继续显示：

```text
看多已满足
看空已满足
方向触发已满足
```

---

## 5.3 双重门禁：生成前阻断 + 推送前阻断

只在 `swing_detector` 阻断还不够，因为以后可能有其他模块生成 hit。

建议两层都加：

### 第一层：`swing_detector.run_all_triggers()`

在生成方向 / 买入 / 抄底 / 止盈类信号前检查 data_quality。

坏数据时：

```text
不生成 A 类 hit
只允许 B 类风险 hit
```

### 第二层：`pusher.format_trigger_message()`

作为最后一道防线。

即使错误 hit 被生成，只要 data_quality 不可信：

```text
不推送 A 类信号
不生成 order button
记录 blocked_signal
```

---

## 5.4 按钮门禁单独实现

一键按钮比文字建议危险，必须单独加规则。

规则：

```text
只要 data_quality.ok != True，任何 order button 都不能生成。
```

按钮生成前必须确认：

- 数据新鲜
- K 线属于当前交易日 / 当前 session
- 当前 session 支持该订单类型
- 夜盘只允许限价单
- 夜盘不支持卖空时不能生成做空类按钮
- 账户现金来自真实 `fetch_account()`
- follower 方向没有冲突
- 不是基于冻结指标

---

## 5.5 Session-aware 策略降级

不要禁止夜盘 / 盘前 / 盘后，但要降级。

建议：

### regular

- 允许完整方向信号
- 允许常规 RSI / VWAP / vol_ratio 逻辑
- 允许较高 confidence

### premarket

- 允许轻量方向判断
- confidence 上限降低，例如 75%
- 文案必须提示盘前流动性风险

### afterhours

- 允许风险提醒和轻量趋势
- 不建议重仓
- confidence 上限降低

### overnight

- 默认只允许观察型信号
- 除非确认 overnight K_5M 实时更新
- 默认不推强烈买入 / 强烈做空
- 文案提示夜盘价差和成交风险

示例：

```text
夜盘流动性较低，价差可能放大。
当前信号仅作观察，不建议重仓跟随。
```

---

## 5.6 Heartbeat 坏数据模式

坏数据时 heartbeat 应切换到 `DATA_UNTRUSTED`。

不应该继续显示：

```text
看空已满足
看多已满足
```

建议格式：

```text
⚠️ 数据状态：不可信
原因：K_5M 最后一根为 2026-05-15 16:00，当前为 2026-05-18

方向信号：已暂停
买入按钮：已禁用
允许推送：持仓止损 / 账户风险

当前持仓：
RKLZ 500股，浮亏 -9.23%

结论：
请以券商实时行情人工判断，系统暂不提供方向建议。
```

---

## 5.7 数据异常告警与恢复机制

建议新增 `DataQualityMonitor`。

逻辑：

- 连续若干轮 data_quality BLOCK
- 或连续 N 分钟 K_5M 不更新
- 推一次 Telegram 告警
- 30 分钟冷却
- 恢复时要求连续 3-5 次正常采样后才发恢复通知

告警示例：

```text
⚠️ 数据异常，方向 / 买卖信号已暂停

原因：
K_5M 最后一根为 2026-05-15 16:00
当前为 2026-05-18，指标已过期

影响：
RSI / VWAP / 量比不可用于方向判断

系统处理：
已暂停方向信号和一键买入按钮
仍保留持仓止损 / 账户风险提醒

建议：
检查 Futu OpenD / K_5M 订阅 / 网络连接
```

恢复示例：

```text
✅ 数据恢复

K_5M 已连续 5 次更新
方向信号将在影子模式观察后恢复
```

注意：恢复后不建议立刻全面恢复强信号，最好先进入影子模式。

---

## 5.8 信号后持仓看护

建议新增：

```text
SignalFollowupMonitor
```

目的：

系统刚推荐 RKLZ / RKLX 后，如果用户真的持有了对应 follower，系统要主动监控“上一条建议是否失效”。

逻辑：

1. 系统推送 RKLZ / RKLX 建议
2. 一段时间内检测到用户持有对应 follower
3. 建立 follow-up 状态
4. 如果 RKLB 朝相反方向走超过 1.5%
5. 或 follower 浮亏超过 1%
6. 推送“上一条方向可能失效”

示例：

```text
⚠️ 上一条看空信号可能失效

RKLB 已从 $123.60 反弹到 $126.10
RKLZ 持仓开始亏损

结论：
暂停加仓，检查是否需要减仓 / 止损。
这不是新的方向建议，而是对上一条信号的风险复核。
```

这能解决 05-18 “系统推完方向后 4 小时没有意识到自己可能错了”的问题。

---

## 5.9 05-18 回归测试必须永久保留

建议永久保留三个事故样本：

```text
tests/fixtures/review_2026_05_14_triggers.json
tests/fixtures/review_2026_05_16_triggers.json
tests/fixtures/review_2026_05_18_triggers.json
```

### 05-14 测试目标

- 上涨趋势中 `target_advance` / `profit_target_hit` 不应过多
- 趋势锁定期间不应反复推卖出
- 应改成“目标上移，继续持有”

### 05-16 测试目标

- 弱势行情中 `near_support` / `swing_bottom` 应明显减少
- 弱势行情不允许轻易抄底
- `swing_bottom WEAK` 只记录不推送

### 05-18 测试目标

必须新增：

1. `is_today=false` 时，`direction_trend` 必须被阻断
2. K 线最后时间为 05-15 时，05-18 不允许生成方向信号
3. RSI=43.1 / vol_ratio=4.94 连续重复时必须进入 stale
4. 坏数据下 `stop_loss_warning` 仍可推，但必须标记数据不可用
5. 坏数据下 Telegram order button 不得出现
6. heartbeat 不得显示“看空已满足”
7. pusher 最终门禁必须能挡住错误 hit

---

## 5.10 影子模式与灰度上线

建议上线流程：

1. 先合入数据门禁，不改策略阈值
2. 单元测试通过
3. 三个事故 fixture 回归通过
4. 方向 / 买入类信号先进入影子模式 24 小时
5. Telegram 只保留：
   - 数据异常
   - 持仓风险
   - heartbeat 状态
6. 第二天人工对比 shadow signals 与真实走势
7. 确认数据门禁稳定后，恢复方向推送
8. 一键订单按钮最后恢复

不要第一天就恢复：

```text
强烈方向信号
大仓位建议
一键买入按钮
```

---

## 6. 我与 Claude 分析的相同点与差异

### 相同点

我认同 Claude 的这些判断：

- 冻结数据必须阻断
- 需要数据异常告警
- 需要 shadow mode
- 需要 05-18 回归样本
- 不能继续让冻结指标生成高信心信号
- 不应再只调策略阈值

### 差异 1：核心原因不是“休市”

Claude 如果把事故归因成“休市推信号”，需要修正。

富途支持 24/5，美股白天也可能可交易。

核心不是：

```text
这个时间不能交易
```

而是：

```text
这个时间系统拿不到可信的新鲜指标，却仍然推了强信号。
```

### 差异 2：黑名单不是主修复

`(43.1, 4.94)` 这类冻结组合可以加，但只能作为临时保险。

真正可靠的是：

- K 线时间
- 当前 session
- 当前交易日
- 指标更新时间
- 连续重复检测
- VWAP 是否真实有效

### 差异 3：pusher 门禁必须存在，但不能只靠 pusher

Claude 提到在 `pusher.py` 第一行做最后防线，这个方向对。

但还需要：

- 生成前阻断
- 推送前阻断
- 按钮前阻断
- heartbeat 展示阻断

四层都要有。

---

## 7. 建议的实施顺序

优先级从高到低：

### P0：必须立刻做

1. 新增 `DataQualityGate`
2. `swing_detector` 前置阻断坏数据方向信号
3. `pusher` 最终阻断坏数据信号
4. 坏数据时禁用 order button
5. `heartbeat` 增加 `DATA_UNTRUSTED` 模式
6. 05-18 fixture 回归测试

### P1：紧接着做

1. `DataQualityMonitor` 告警 / 恢复机制
2. `SignalFollowupMonitor` 信号后持仓看护
3. session-aware confidence 降级
4. shadow mode

### P2：后续专题

1. K_5M 冻结根因排查
2. Futu 订阅自动恢复
3. overnight 数据源适配
4. VWAP 按 session 分段计算
5. version consistency 检查

---

## 8. 最终建议

下一步不要继续加新策略，也不要继续微调信号阈值。

建议只做防御层：

```text
数据门禁
按钮门禁
心跳坏数据模式
05-18 回归测试
影子模式
```

系统当前最重要的目标不是“更聪明”，而是：

```text
不再基于不可信数据误导用户。
```

只有做到这一点，后面的趋势策略、止盈策略、follower 合并、弱势行情过滤才有意义。

