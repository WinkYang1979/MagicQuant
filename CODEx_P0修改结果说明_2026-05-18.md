# MagicQuant P0 数据门禁修改结果说明（Codex）

日期：2026-05-18  
范围：05-18 冻结 K_5M 误推强烈方向信号事故的 P0 防御修复  

## 1. 本次目标

本次没有继续优化策略阈值，重点是防止系统再次基于过期 / 冻结指标生成交易建议。

目标：

- K_5M 非当前交易日时，阻断方向 / 买入 / 止盈类信号。
- RSI + vol_ratio 连续重复时，进入数据不可信状态。
- `swing_detector` 前置阻断，`pusher` 最终阻断。
- 坏数据下不生成 Telegram order button。
- 坏数据下 `stop_loss_warning` 仍可推，但要标记技术指标不可用。
- heartbeat 进入 `DATA_UNTRUSTED` 模式，不再显示“看空已满足 / 看多已满足”。
- 加入 05-18 永久回归 fixture。

## 2. 修改文件

### 新增

- `core/focus/data_quality.py`
- `tests/fixtures/review_2026_05_18_triggers.json`
- `CODEx_P0修改结果说明_2026-05-18.md`

### 修改

- `core/focus/swing_detector.py`
- `core/focus/pusher.py`
- `core/focus/heartbeat.py`
- `core/focus/focus_manager.py`
- `tests/test_signal_upgrade_v0_5_34.py`

## 3. 新增 DataQualityGate

新增 `core/focus/data_quality.py`。

核心函数：

```python
evaluate_data_quality(session, indicators, now=None)
```

返回：

```python
DataQuality(
    ok=False,
    level="BLOCK",
    reason="...",
    can_direction=False,
    can_entry=False,
    can_exit=False,
    can_risk=True,
    can_heartbeat=True,
)
```

检查项：

- `indicators.data_ok`
- `indicators.is_today`
- 最近 K_5M 时间是否属于当前交易日 / 当前 overnight session
- 最近 K_5M 是否超过 10 分钟未更新
- RSI + vol_ratio 是否连续 3 次完全重复

注意：

- 默认当前时间使用 `America/New_York`，避免本机 Sydney 时间和美股 K 线时间直接比较导致误判。
- `stop_loss_warning` 被归为风险类，在坏数据下仍允许。

## 4. swing_detector 前置门禁

在 `run_all_triggers()` 入口加入：

- `evaluate_data_quality(...)`
- `session._last_data_quality`
- `session._blocked_signals`

坏数据时：

- 不再生成 `direction_trend / swing_bottom / near_support / rapid_move / target_advance / profit_target_hit`
- 只检查并返回 `stop_loss_warning`
- 如果没有 stop-loss 风险，返回空列表

这样 05-18 的第一条 14:03 `direction_trend short STRONG` 会在生成阶段就被挡住。

## 5. pusher 最终门禁

在 `format_trigger_message()` 加最终防线。

如果实时 `session._last_indicators_cache` 判定坏数据：

- 方向 / 买入 / 止盈类 trigger 直接返回 `None`
- `focus_manager` 现在会跳过 `None`，不发送 Telegram
- `stop_loss_warning` 可以继续推送
- 但 `buttons=[]`
- 并在消息中追加：

```text
⚠️ 技术指标数据不可用
原因: ...
本提醒仅基于实时价格/持仓盈亏，不代表方向判断。
```

这解决“一键买入 RKLZ”按钮在坏数据下仍出现的问题。

## 6. heartbeat DATA_UNTRUSTED

`heartbeat.py` 增加数据质量判断。

坏数据时新增展示：

```text
⚠️ 数据状态: 不可信
原因: ...
K_5M last: ...
方向信号: 已暂停
买入按钮: 已禁用
允许推送: 持仓止损 / 账户风险

🎯 触发器距离: 已隐藏(数据不可信)
```

并且提前返回，不再继续展示“看空已满足 / 看多已满足”等触发器距离。

## 7. 05-18 回归测试

新增 fixture：

```text
tests/fixtures/review_2026_05_18_triggers.json
```

新增测试：

1. 05-18 第一条坏数据 `direction_trend` 必须被 DataQualityGate 阻断。
2. pusher 最终门禁必须返回 `None`，不生成消息和按钮。
3. 坏数据下 `stop_loss_warning` 仍可推送，但 `buttons=[]` 且文案标注技术指标不可用。

## 8. 验证结果

已通过：

```text
python -B -m unittest tests.test_signal_upgrade_v0_5_34
Ran 7 tests
OK
```

已通过语法编译检查：

```text
compile ok
```

额外尝试运行：

```text
tests.test_target_advance
tests.test_loss_alert_passthrough
tests.test_target_follower_fix
```

结果：未能运行，原因是当前 Codex bundled Python 环境没有安装 `moomoo/futu`，这些测试通过 `core.focus.__init__` 导入 `focus_manager.py` 时依赖券商 SDK。该失败不是本次代码语法问题。

## 9. 尚未做的 P1 / P2

本次只做 P0。

未做：

- Telegram DataQualityMonitor 告警 / 恢复通知
- SignalFollowupMonitor 信号后持仓看护
- session-aware confidence 降级
- 影子模式上线
- K_5M 冻结根因排查
- session 分段 VWAP
- overnight 数据源适配

## 10. 给 Claude 复盘时重点检查

请重点复核：

1. `DataQualityGate` 的 10 分钟 K_5M 新鲜度阈值是否合理。
2. overnight session 的交易日归属判断是否还需要更精细。
3. `stop_loss_warning` 坏数据下保留但去按钮，这个取舍是否合适。
4. pusher 返回 `None` 后，所有调用方是否都能安全跳过。
5. 是否需要把 `_blocked_signals` 进一步落盘到 `data/review/YYYY-MM-DD/blocked_signals.json`。

